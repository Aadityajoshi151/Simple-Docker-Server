{
  "name": "simple-docker-server",
  "version": "1.0.0",
  "description": "For simple hobby projects, simply spin up a server in a docker container.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
